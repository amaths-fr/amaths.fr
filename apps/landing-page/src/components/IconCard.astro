---
import { Icon } from "astro-icon/components";

interface Props {
  icon: string;
  title: string;
  description: string;
  variant?: "default" | "contrasted";
}

const { icon, title, description, variant = "default" } = Astro.props;
---

<div class:list={["container", variant]}>
  <div class:list={["icon", variant]}>
    <Icon name={icon} width="1.5rem" height="1.5rem" />
  </div>
  <div class="text">
    <h4>{title}</h4>
    <p>{description}</p>
  </div>
</div>

<style>
  .container {
    border: 1px solid var(--elevation-2);
    background-color: var(--elevation-1);
    padding: var(--s32);
    border-radius: var(--r16);
    display: flex;
    flex-direction: column;
    gap: var(--s24);
  }

  .container.contrasted {
    background: var(--section-gradient-3);
  }

  .icon {
    padding: var(--s12);
    background-color: var(--elevation-3);
    width: fit-content;
    border-radius: var(--r8);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .icon.contrasted {
    background-color: #ebbe3633;
  }

  .text {
    display: flex;
    flex-direction: column;
    gap: var(--s12);
  }

  p {
    color: var(--primary-color-tern);
  }
</style>
